// Test básico de humo para Flixboy.
//
// NOTA: FlixboyApp inicializa Firebase en main(), por lo que un widget test
// completo requeriría mockear Firebase. Este test solo verifica que el
// proyecto compila y que los tests corren sin errores de referencia.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder — reemplazar con tests reales de Flixboy', () {
    expect(1 + 1, 2);
  });
}
